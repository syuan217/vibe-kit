# <模块名>

- 职责:<一句话>
- 代码根目录:`<src/...>`

## 关键文件

| 文件 | 作用 |
|---|---|
| `<路径>` | <作用> |

## 入口点

<该模块被触发的方式:HTTP 路由 / 消息订阅 / 定时任务,及各自所在文件>

## 主要流程

<核心调用链,如:`OrderController.create` → `OrderService.create` → `OrderRepo.save` + 发送 `order.created` 事件>

## 数据

<涉及的表/实体及其定义文件>

## 常见修改场景

| 要做什么 | 改哪里 |
|---|---|
| <如:新增一个订单查询接口> | `<controller 路径>` + `<service 路径>` |
| <如:订单新增一个状态> | `<枚举路径>` + `<状态机路径>` + 迁移脚本 |
