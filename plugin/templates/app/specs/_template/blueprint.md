# NNN-需求名 — 技术方案(给 AI 执行)

> 本文件面向 **AI**(实现指引)。结构化但措辞向"可读"妥协——目标是"AI 读完能动手",不是"可直接粘贴的代码"。
> 实现以本文件为准;vibe-build 偏离处记进 `spec.md`「实现偏差」,vibe-verify 核对一致性。
> 跨应用需求:本服务的契约约束来自 hub 总 spec(见 `spec.md` 首行链接)。

## 1. 技术选型与理由

<采用什么方案、为什么这么选(对比过的备选、关键约束)。>

## 2. 数据模型

<新增/变更的数据结构(表、字段、消息体)。标注迁移与兼容性。>

## 3. 对外契约变更

<新增/修改的 API、消息、事件。标注兼容/破坏性。跨应用需求的契约以 hub 总 spec 契约章节为准。>

## 4. 任务清单(垂直切片)

> 每片可独立实现 + 独立验证(vibe-build 逐片实现,不一次性全做完再测)。每片带验收检查点。

- [ ] 切片 1:<名称>
  - 内容:
  - 验收检查点:
- [ ] 切片 2:<名称>
  - 内容:
  - 验收检查点:
